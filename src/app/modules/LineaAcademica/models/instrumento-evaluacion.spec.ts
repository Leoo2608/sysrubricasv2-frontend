import { InstrumentoEvaluacion } from './instrumento-evaluacion';

describe('InstrumentoEvaluacion', () => {
  it('should create an instance', () => {
    expect(new InstrumentoEvaluacion()).toBeTruthy();
  });
});
